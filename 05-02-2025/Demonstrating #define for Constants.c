#include <stdio.h>

#define PI 3.14159  // Defining a constant

int main() {
    float radius = 5.0;
    float area = PI * radius * radius;  // Using the constant
    printf("Area of Circle: %.2f\n", area);
    return 0;
}
