#include <stdio.h>

int 9number = 50; // Invalid: Can't start with a number
int char = 100;   // Invalid: Reserved keyword

int main() {
    printf("This program will not compile due to invalid identifiers.");
    return 0;
}
