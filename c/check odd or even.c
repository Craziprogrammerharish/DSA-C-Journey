#include <stdio.h>

int main() {
    int num = 7;
    
    // This program checks whether a number is even or odd.
    
    if (num % 2 == 0)
        printf("%d is even\n", num);
    else
        printf("%d is odd\n", num);
    return 0;
}
