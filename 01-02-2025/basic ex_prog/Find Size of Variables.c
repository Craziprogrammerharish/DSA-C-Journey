#include <stdio.h>

int main() {
    char ch = 'A';
    int num = 25;
    printf("Size of ch variable: %lu bytes\n", sizeof(ch));
    printf("Size of num variable: %lu bytes\n", sizeof(num));
    return 0;
}
