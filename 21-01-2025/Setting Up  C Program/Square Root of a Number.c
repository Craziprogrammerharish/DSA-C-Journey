#include <stdio.h>
#include <math.h>
int main() {
    double num = 25.0, sqrt_result;
    sqrt_result = sqrt(num);  // Calculating square root
    printf("Square root of %.2f is %.2f\n", num, sqrt_result);
    return 0;
}
