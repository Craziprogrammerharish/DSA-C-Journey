#include <stdio.h>
#define RATE 50 // Define a constant

int main() {
    int quantity = 3; // Declare variable
    int total = quantity * RATE; // Use variable and constant
    printf("Total Cost: %d\n", total); // Display result
    return 0;
}
