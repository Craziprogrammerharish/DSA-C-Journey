#include <stdio.h>

int main() {
    int count = 10; // Variable declaration
    printf("Initial Count: %d\n", count);
    count += 5; // Modify the variable
    printf("Updated Count: %d\n", count);
    return 0;
}
